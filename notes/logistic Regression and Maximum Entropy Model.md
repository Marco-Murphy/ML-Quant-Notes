# **笔记：《统计学习方法》之逻辑回归与最大熵模型**

## 目录

-概述

-逻辑回归

-最大熵模型

-小故事：关于局部最优和全局最优

## 1. 概述
   
把算法发展的过程当作不断为了解决问题而填坑，填坑之后又挖坑，如此反复的过程就好了。新算法的产生都是为了解决一些旧算法的缺陷或者说不足。

比如感知机，它的缺陷是，只能处理线性可分的数据，并且它对于”更新“的过程略显粗暴了——对所有分量平等地往正确方向拉，虽然最后能到正确位置，但是过程略显冗余。

除此之外感知机的输出很粗暴，只有两类，没有概率，跟部分实际应用脱节了。

## 2. 模型：逻辑回归

1. 从感知机的引入

在学习逻辑回归之前，我们先简单复习一下感知机的内容。感知机也是一类分类模型，但是它只能处理线性可分数据，并且只能输出是或不是的死板结果（位于超平面的哪一侧）

在实际情景中，我们会发现我们对于模型的要求不止这么简单，我们希望得到具体的概率。那么能用什么来连接原有的分类和概率呢？一个自然的想法是，距离。

在感知机中，同一侧样本关于超平面的距离是不一致的，这个距离是不同样本之间的差值，它可以变成一个具体的量——可信度，也就是概率的雏形。

具体来说就是，距离超平面越近，越容易被误分类，越不可信，反之亦然。那么，有了这个置信度，怎么变成概率呢？

2. 数学推导与变形

首先，距离的值域是负无穷到正无穷，概率的值域是（0，1）

我们的目标是使得二者的值域在经过一系列变换后相等。

我们先介绍“几率”这个概念，几率指的是，一件事发生的概率（p）/一件事不发生的积累（1-p）。这个定义可以在

一定程度上等价于概率。几率的值域是0到正无穷，这比概率更加灵活。接着，我们再对几率取对数，得到的几率对数的值域就是负无穷到正无穷。

如此一来，这个过程就被打通了，我们按照上述过程逆运算，就可以把距离变成概率了。

最后的二元逻辑斯谛表达式是：

对于二分类问题，输出 $y \in \{0,1\}$，给定输入 $\mathbf{x}$，模型定义为：

$$
P(y=1 \mid \mathbf{x}) = \frac{\exp(\mathbf{w}^\top \mathbf{x} + b)}{1 + \exp(\mathbf{w}^\top \mathbf{x} + b)}
$$

$$
P(y=0 \mid \mathbf{x}) = \frac{1}{1 + \exp(\mathbf{w}^\top \mathbf{x} + b)}
$$

其中 $\mathbf{w}$ 为权重向量，$b$ 为偏置项。

需要补充的是，二元逻辑斯谛是逻辑斯谛分布的参数化形式。逻辑斯谛分布的数学表达式如下：

逻辑斯蒂分布是一种连续概率分布，其累积分布函数为逻辑斯蒂函数。

概率密度函数 (PDF)

对于位置参数 $\mu \in \mathbb{R}$ 和尺度参数 $s > 0$，概率密度函数为：

$$
f(x; \mu, s) = \frac{\exp\left(-\frac{x-\mu}{s}\right)}{s \left(1 + \exp\left(-\frac{x-\mu}{s}\right)\right)^2}
$$

等价形式：

$$
f(x; \mu, s) = \frac{1}{s} \cdot \frac{e^{-\frac{x-\mu}{s}}}{\left(1 + e^{-\frac{x-\mu}{s}}\right)^2}
$$

累积分布函数 (CDF)

累积分布函数为：

$$
F(x; \mu, s) = \frac{1}{1 + \exp\left(-\frac{x-\mu}{s}\right)} = \sigma\left(\frac{x-\mu}{s}\right)
$$

其中 $\sigma(\cdot)$ 是 Sigmoid 函数。

标准形式

当 $\mu = 0$，$s = 1$ 时，为标准逻辑斯蒂分布：

$$
f(x) = \frac{e^{-x}}{(1+e^{-x})^2}, \quad F(x) = \frac{1}{1+e^{-x}}
$$

3. 模型参数估计——求解w和b

核心思路：利用历史数据，反推出最优的w和b

方法：极大似然估计法

含义：写出似然函数，即历史数据所构成的情况的表达式，再求出这个表达式的最大值即可。

意为找到相应的参数，使得符合历史数据的情况最有可能发生。
   
大致步骤为：写出似然函数表达式→取该表达式的对数→求该式子的最大值→求出参数w和b

具体过程如下

### 1. 设未知数

对于二分类问题，设数据集为 $\{(\mathbf{x}^{(i)}, y^{(i)})\}_{i=1}^{m}$，其中 $\mathbf{x}^{(i)} \in \mathbb{R}^n$，$y^{(i)} \in \{0,1\}$。

模型参数：权重向量 $\mathbf{w} = (w_1, w_2, \dots, w_n)^\top$，偏置 $b$。

模型假设（条件概率）：
$$
\begin{aligned}
P(y=1 \mid \mathbf{x}; \mathbf{w}, b) &= \frac{1}{1 + e^{-(\mathbf{w}^\top \mathbf{x} + b)}} \triangleq h(\mathbf{x}) \\
P(y=0 \mid \mathbf{x}; \mathbf{w}, b) &= 1 - h(\mathbf{x})
\end{aligned}
$$

### 2. 写出似然函数

由于样本独立同分布，似然函数为所有样本联合概率：

$$
L(\mathbf{w}, b) = \prod_{i=1}^{m} \left[ h(\mathbf{x}^{(i)})^{y^{(i)}} \cdot (1 - h(\mathbf{x}^{(i)}))^{1 - y^{(i)}} \right]
$$

### 3. 取对数（对数似然）

取自然对数将乘积转化为求和：

$$
\ell(\mathbf{w}, b) = \ln L(\mathbf{w}, b) = \sum_{i=1}^{m} \left[ y^{(i)} \ln h(\mathbf{x}^{(i)}) + (1 - y^{(i)}) \ln (1 - h(\mathbf{x}^{(i)})) \right]
$$

将 $h(\mathbf{x}) = \frac{1}{1+e^{-z}}$，其中 $z = \mathbf{w}^\top \mathbf{x} + b$，代入并化简得：

$$
\ell(\mathbf{w}, b) = \sum_{i=1}^{m} \left[ y^{(i)} (\mathbf{w}^\top \mathbf{x}^{(i)} + b) - \ln(1 + e^{\mathbf{w}^\top \mathbf{x}^{(i)} + b}) \right]
$$

### 4. 求解最大值（求导并令梯度为零）

最大化 $\ell(\mathbf{w}, b)$ 等价于最小化负对数似然。对参数求偏导：

- 对 $w_j$ 求导：
  $$
  \frac{\partial \ell}{\partial w_j} = \sum_{i=1}^{m} \left( y^{(i)} - h(\mathbf{x}^{(i)}) \right) x_j^{(i)}
  $$

- 对 $b$ 求导：
  $$
  \frac{\partial \ell}{\partial b} = \sum_{i=1}^{m} \left( y^{(i)} - h(\mathbf{x}^{(i)}) \right)
  $$

令梯度为零，得到似然方程组：

$$
\begin{cases}
\displaystyle \sum_{i=1}^{m} \left( y^{(i)} - h(\mathbf{x}^{(i)}) \right) x_j^{(i)} = 0, & j=1,\dots,n \\[1.5em]
\displaystyle \sum_{i=1}^{m} \left( y^{(i)} - h(\mathbf{x}^{(i)}) \right) = 0
\end{cases}
$$

### 5. 反推 $\mathbf{w}$ 和 $b$

上述方程组没有闭式解析解（因为 $h(\mathbf{x}^{(i)})$ 是 $\mathbf{w}, b$ 的非线性 Sigmoid 函数），需使用数值优化算法（如梯度上升法）迭代求解。

梯度上升更新公式（学习率 $\alpha$）：

$$
\begin{aligned}
w_j &\leftarrow w_j + \alpha \frac{\partial \ell}{\partial w_j} = w_j + \alpha \sum_{i=1}^{m} \left( y^{(i)} - h(\mathbf{x}^{(i)}) \right) x_j^{(i)} \\
b &\leftarrow b + \alpha \frac{\partial \ell}{\partial b} = b + \alpha \sum_{i=1}^{m} \left( y^{(i)} - h(\mathbf{x}^{(i)}) \right)
\end{aligned}
$$

重复迭代直至收敛，得到最终参数 $\mathbf{w}^*$ 和 $b^*$。

> 注：实际应用中常采用批量梯度下降（负梯度方向）、随机梯度下降或拟牛顿法等优化策略。

## 3. 关于概率的小补充————其它量化概率的思路

基于树，基于神经网络，基于核

## 4. 策略
  
何为策略？策略就是如何评价模型的一个指标，也叫损失函数。

这个指标具有以下特征：

1.通过这个指标反映模型对目标的完成效果。

2.该指标与模型的参数直接要有相关性，在感知机中，该指标（损失函数）就是w和b的连续可导函数。

结合感知机“分水果”这一动作，我们很自然地想到，利用“错分水果的数量”来衡量感知机对目标的完成效果。但仔细一想，这不好。

为什么呢？

放到数学情景中你就知道了。
  
误分类点数这一数值与感知机输出的参数毫无关联，想要知道有多少个点被误分类我们只能一个个去数，去算，我们并不能将其与w和b关联起来。

因此，我们想到了利用误分类点到超平面的总距离来作为策略——距离可以反映感知机的分类情况，如果总距离很大，证明这时候的参数离正确分类还很远，还得调。如果总距离很小，那么就说明接近正确参数了。如果总距离为0，就说明所有点都是被正确分类的了，目标完成。
  
更重要的是，用总距离这个指标，可以让w和b也参与进来了，公式为：(1/||w||)*|wx+b|。其中||w||是w的L2范数，也就是距离的意思。
  
  !!!(缺少说明，为什么可以丢掉范数)
  通过这个指标，我们可以建立结果与标准的关系，进行迭代
 
所以，感知机的损失函数是：$ L(w, b) = -\sum_{x_i \in M} y_i (w \cdot x_i + b)$    

我们的目标是使损失函数为0。

## 5. 算法
  
何为算法？算法就是在符合策略的条件下得到最佳模型的方法

感知机采用的算法是随机梯度下降法。把感知机求解的过程想象成下山，随机梯度下降就是找到坡度最陡的方向进而向该方向移动。每移动一步，就再找最陡方向，直到下山

梯度的数学含义是什么呢？其实是损失函数对w和b求偏导的向量形式。

首先解释下偏导，偏导的意思是，在一个具有很多变量的式子里，将剩下的变量当作常量，只对一个变量求导。偏导的目的是判断每个变量的重要程度，最重要的部分相当于“最陡”的方向

w和b的梯度公式：
$$
\frac{\partial L}{\partial w} = -\sum_{x_i \in M} y_i x_i
$$ 

$$
\frac{\partial L}{\partial b} = -\sum_{x_i \in M} y_i 
$$ 

接着，朝着最陡的方向移动，在数学上也就是对w和b进行更新。
$$
w \leftarrow w + \eta \sum_{x_i \in M} y_i x_i 
$$ 

$$
b \leftarrow b + \eta \sum_{x_i \in M} y_i 
$$  
其中，eta是学习率，也就是步长。即每次往最陡的方向移动多少。学习率越低，训练的结果越准确。

## 5. 对偶形式

## 6. 收敛性
收敛性是什么？有什么用？简单来说，收敛性就是证明“感知机不会无限循环，而是只要满足了特定情况就必定可以中止的必要性证明。

其公式为： $k \leq \left(\frac{\gamma}{R}\right)^2$

其中，k表示更新次数，R表示数据集中离原点最远的点到原点的距离，γ是正负两类样本之间的最小间距。

这个公式说明了，假如两类数据分的很开，那感知机找到结果的次数就少，反之亦然。
      
